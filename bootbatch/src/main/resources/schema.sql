CREATE TABLE IF NOT EXISTS PRODUCTTS
(
    product_id INT PRIMARY KEY,
    title VARCHAR(200),
    description VARCHAR(500),
    price VARCHAR(10),
    discount VARCHAR(2),
    discounted_price VARCHAR(10)
    );
